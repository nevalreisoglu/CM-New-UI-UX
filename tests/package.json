{
  "name": "ecm-journey-studio-tests",
  "private": true,
  "description": "Playwright regression scripts for the ECM Journey Studio prototype",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "shots": "playwright test specs/screenshots.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
